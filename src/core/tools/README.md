This directory contains several auxiliary classes used internally by the core for things such as:

- SHA1 hashing
- Reference counting
- String tokenization
- Abstracting OS-specific shared libraries
