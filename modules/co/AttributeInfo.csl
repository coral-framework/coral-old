/*
	Offers type introspection for an attribute, as a member of an AttributeContainer.
 */
interface AttributeInfo : MemberInfo
{
	// The attribute's type.
	readonly attribute Type type;

	/*
		Whether the attribute is read-only.
		\note For structs, this is always false (i.e. structs are always read/write).
	 */
	readonly attribute bool isReadOnly;
};
