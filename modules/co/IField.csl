/*
	Offers type introspection for a field, as a member of an IRecordType.
 */
interface IField extends IMember
{
	// The field type.
	readonly IType type;

	/*
		Whether the field is read-only.
		\note For struct fields, this is always false.
	 */
	readonly bool isReadOnly;
};
