/*
	Describes a field as a member of an IRecordType.

	\note The term \e field usually denotes a <em>member variable</em>, but in Coral
		this term has been generalized to also refer to <em>interface properties</em>.
 */
interface IField extends IMember
{
	// The field type.
	readonly IType type;

	// Whether the field is read-only (for struct fields, always false).
	readonly bool isReadOnly;
};
