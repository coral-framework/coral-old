/*
	Type representing a native class (a C++ class wrapped as a Coral value).
 */
interface INativeClass extends IClassType
{
	// Name of the header file where the native C++ type is defined.
	readonly string nativeHeader;

	// Fully-qualified name of the native C++ type.
	readonly string nativeName;
};
