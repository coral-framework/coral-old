/*
	A dynamic component, with interfaces defined in run-time and methods implemented in Lua.
 */
component Component
{
	// Handles requests to the proxy interfaces created for this component.
	provides co.DynamicProxyHandler dynamicProxyHandler;

	// This component has its own built-in reflector.
	provides co.Reflector reflector;
};
