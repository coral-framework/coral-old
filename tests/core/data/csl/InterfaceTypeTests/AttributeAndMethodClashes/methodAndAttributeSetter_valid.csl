// This test interface should not raise an exeption as the attribute setter 
// does not exists and it doesn't clashes with the name of the method.
interface methodAndAttributeSetter_valid 
{
	void setBirthDate();
	void isBirthDate();
	readonly attribute string birthDate;
};


