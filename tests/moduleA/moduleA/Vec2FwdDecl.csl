/*
	Proof-of-concept native class that wraps a std::pair<double,double>.
 */
@co.Include( "moduleA/Vec2D.h" )
native class Vec2FwdDecl
{
	<c++
		namespace moduleA {
			#ifdef MODULEA_VEC2_INCLUDED
				struct Vec2FwdDecl : Vec2D {};
			#else
				struct Vec2FwdDecl;
			#endif
		} // namespace moduleA
	c++>
};
