/*
	Proof-of-concept native class that wraps a std::pair<float,float>.
 */
@co.Include( "utility" )
native class Vec2F
{
	<c++
		namespace std {
			template<class T1, class T2> struct pair;
		} // namespace std
		namespace moduleA {
			typedef std::pair<float, float> Vec2F;
		} // namespace moduleA
	c++>
};
